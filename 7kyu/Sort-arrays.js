//❓Простой пример использования сортировки. Создайте функцию, которая возвращает элементы входного массива/списка, отсортированные в лексикографическом порядке.

// ✅ SOLUTION:
// input: names - unsorted array
// output: sorted array
// sortme = function( names ){
//
// }

const sortme = (names) => {
    return names.sort();
}
console.log(sortme(['one', 'two', 'three' ]));
